D:                -> change disk
cd path/          -> go to some folder
ls -la            -> list files with info

------------
cat >> README.md  -> create new file
input some data   -> data will be written to file
Ctrl + D          -> save and exit

cat >> README.md  -> second time used command will modify your file by adding extra text on the end of file

_________________________________

Global config

git config --global user.name "VR-Java"
git config --global user.email "email..."

_________________________________

create folder for Git
git init      -> 
git status    -> shows all files status
git add .     -> add all files

git remote add origin https://github.com/VR-Java/LGS-Java.git -> connect folder on computer with Github repository

git commit -m "message"
git push origin XXXX -> XXXX=branch


__________________________________

push project to existing Git repository:

git init
git add .
git commit -m "my commit"
git remote set-url origin git@github.com:username/repo.git
// create new branch or...
git push origin master


